import { CoordonneePipe } from './coordonnee.pipe';

describe('CoordonneePipe', () => {
  it('create an instance', () => {
    const pipe = new CoordonneePipe();
    expect(pipe).toBeTruthy();
  });
});
